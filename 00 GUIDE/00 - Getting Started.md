# Getting Started

# Azure Logic Apps Lab Setup

From the terminal run the command `azd up` to provision the environment. On first run, you'll be asked for the environment name to use. 

It's recommended to uuse the name logicapps and  your initials for the environment. For example `logicapps-rm`